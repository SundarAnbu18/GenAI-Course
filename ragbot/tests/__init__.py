"""Test suite for the RAG chatbot.

Run everything with:  python manage.py test

Nothing here loads the embedding model or calls the Anthropic API — the slow
and paid parts are mocked, so the suite runs in under a second.
"""
